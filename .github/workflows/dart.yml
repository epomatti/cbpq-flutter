name: Flutter

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    container:
      image:  cirrusci/flutter
    
    steps:
    - uses: actions/checkout@v1
    - name: print user
      run: echo $USER
    - name: print username
      run: echo $USERNAME          
    - name: id
      run: id
    - name: Install dependencies
      run: flutter pub get
    - name: Run tests
      run: flutter test
